#include<stdio.h>
#include<math.h>
#include<strings.h>
#include<limits.h>
int main (){
 struct person{
    int age;
    int salary;
    char name[200];
    }one , two;
    printf("enter");
strcpy(one.name,"steve");
scanf("%d",&two.age);
printf("%d\n",two.age);
printf("%s",one.name);
    return 0;
}