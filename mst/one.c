// #include <stdio.h>

// int main() {
//     char str[100];

//     printf("Enter a string: ");
//     fgets(str, sizeof(str), stdin);

//     for (int i = 0; str[i] != '\0'; i++) {
//         if (str[i] >= 'a' && str[i] <= 'z') {
//             str[i] = str[i] - 'a' + 'A';
//         } else if (str[i] >= 'A' && str[i] <= 'Z') {
//             str[i] = str[i] - 'A' + 'a';
//         }
//     }

//     printf("Flipped case string: %s", str);

//     return 0;
// }
#include <stdio.h>

int main() {
    // int arr[] = {1, 3, 5, 7, 9, 11, 13, 15}; // Sorted array
    // int n = sizeof(arr) / sizeof(arr[0]);
    
    // int temp = 0;

    // for (int i = 0 ; i <= n/2 ; i++) {
    //     temp = arr[i];
    //     arr[i] = arr[n-i-1];
    //     arr[n-i-1] = temp;
    // }

    // for (int i = 0; i < n; i++) {
    //     printf("%d ", arr[i]);
    // }
    
int g;
printf("%d",g);

    return 0;
}